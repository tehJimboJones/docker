http:
  middlewares:
    auth:
      basicAuth:
        users:
        -  'admin:$apr1$UVo6RtsW$A079TecA7I7EL6nFskiQD1'
  routers:
    api:
      rule: Host(`cpi.jimspeglegs.com`)
      entryPoints:
#        - web
        - websecure
      middlewares:
        - auth
      service: api@internal
      tls:
        certResolver: letsencrypt
