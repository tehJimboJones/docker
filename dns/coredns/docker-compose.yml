version: '3.3'
services:
  service-name:
    image: coredns/coredns
    container_name: coredns
    labels:
      - coredns
    volumes:
      - /dev/null:/path/on/container
    ports:
      - "8085:80"
    restart: unless-stopped
    networks:
      web:

networks:
  web:
    external: true
